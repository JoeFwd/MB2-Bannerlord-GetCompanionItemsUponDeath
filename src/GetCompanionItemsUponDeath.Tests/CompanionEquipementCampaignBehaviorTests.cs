using FakeItEasy;
using NUnit.Framework;
using TaleWorlds.CampaignSystem;

namespace GetCompanionItemsUponDeath.Tests
{
    public class CompanionEquipmentCampaignBehaviorTests
    {
        /*[SetUp]
        public void Setup()
        {
        }

        [Test]
        public void RegisterEvents_OnKilledHero_CopiesKilledHeroEquipmentToPlayerPartyInventory()
        {
            CampaignBehaviorBase campaignBehaviorBase = new CompanionEquipmentCampaignBehavior();
                
            Hero
            
            var equipmentTransferHandler = A.Fake<IEquipmentTransferHandler>();

            var target = new ClassToTest(mocked.Object);
            target.DoStuff();

            mocked.Verify(x => x.CallMyMethod(It.IsAny<string>());
        }

        private static Hero getPlayerHero()
        {
            
        }
        
        private static IEquipmentTransferHandler getFakeEquipmentTransferHandler()
        {
            return A.Fake<IEquipmentTransferHandler>(optionBuilder => 
                optionBuilder.WithArgumentsForConstructor(new object[] { })
            );
        }*/
    }
}